<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
  <title>Document</title>
</head>
<body>

  <h4 class="container mt-4"><strong>회원 가입</strong></h4>
  <!-- 이름, 닉네임, 성별, 가족 내 위치, 전화번호 -->
  <div class="container mt-3">
    <form id="signup-form" action="/register" method="POST">
      <div class="form-group">
        <label>이름</label>
        <input type="text" class="form-control" name="이름">
      </div>
      <div class="form-group">
        <label>닉네임</label>
        <input type="text" class="form-control" name="닉네임">
      </div>
      <div class="form-group">
        <label>성별</label><br>
        <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" name="성별" id="inlineRadio1" value="Male">
            <label class="form-check-label" for="inlineRadio1">Male</label>
        </div>
        <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" name="성별" id="inlineRadio2" value="Female">
            <label class="form-check-label" for="inlineRadio2">Female</label>
        </div>
      </div>
      <div class="form-group">
        <label>가족 내 위치</label><br>
        <select class="form-select" aria-label="Default select example" name="위치" id="familyLocation">
            <option selected>Open this select menu</option>
            <option value="부">부</option>
            <option value="모">모</option>
            <option value="자녀">자녀</option>
        </select>
      </div>
      <div class="form-group">
        <label>전화번호</label><br>
        <input type="tel" id="phone" name="전화번호" pattern="[0-9]{11}" required />
        <small>Format: 1234567890</small>
      </div>
      <button type="submit" class="btn btn-outline-secondary">Sign up</button>
    </form>
  </div>

  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/3.1.9-1/crypto-js.js"></script>
  <script>

      $('#signup-form').submit(function(event) {
        event.preventDefault();

        var 이름 = $('input[name="이름"]').val();
        var 닉네임 = $('input[name="닉네임"]').val();
        var gender = document.querySelector('input[name="성별"]:checked').value;
        var familyLocation = document.getElementById('familyLocation').value;
        var 전화번호 = $('input[name="전화번호"]').val();
        console.log(이름, 닉네임, gender, familyLocation, 전화번호);

        // 이름, 닉네임, 성별, 가족 내 위치, 전화번호
        // 서버에 회원 가입 정보를 전송합니다.
        $.ajax({
          type: 'POST',
          url: '/register', // 실제 서버 측 스크립트 경로
          data: {
            name: 이름,
            nick: 닉네임,
            gender: gender,
            familyLocation: familyLocation,
            phone: 전화번호
          },
          success: function(result) {
            // 성공한 경우 처리
            console.log('성공!');
            window.location.href = '/list';
          },
          error: function(xhr, textStatus, errorThrown) {
            console.log('실패');
            alert('사용할 수 없는 ID 입니다!!!');
            window.location.reload(true);
          }
        });
      });
  </script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
</body>
</html>
