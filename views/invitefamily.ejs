<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
  <title>Document</title>
</head>
<body>

  <h4 class="container mt-4"><strong>회원 가입</strong></h4>
  <!-- 이름, 닉네임, 성별, 가족 내 위치, 전화번호 -->
  <div class="container mt-3">
    <form id="signup-form" action="/family/invite" method="POST">
      <div class="form-group">
        <label>가족별명</label>
        <input type="text" class="form-control" value="<%= name %>" name="가족별명" disabled>
      </div>
      <div class="form-group">
        <label>가족 내 위치</label><br>
        <select class="form-select" aria-label="Default select example" name="위치" id="familyLocation">
            <option selected>Open this select menu</option>
            <option value="부">부</option>
            <option value="모">모</option>
            <option value="자녀">자녀</option>
        </select>
      </div>
      <button type="submit" class="btn btn-outline-secondary">생성</button>
    </form>
  </div>

  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/crypto-js/3.1.9-1/crypto-js.js"></script>
  <script>

      $('#signup-form').submit(function(event) {
        event.preventDefault();

        var 이름 = $('input[name="가족별명"]').val();
        var familyLocation = document.getElementById('familyLocation').value;
        console.log(이름, familyLocation);

        // 이름, 닉네임, 성별, 가족 내 위치, 전화번호
        // 서버에 회원 가입 정보를 전송합니다.
        $.ajax({
          type: 'POST',
          url: '/family/invite', // 실제 서버 측 스크립트 경로
          data: {
            familynick: 이름,
            familyLocation: familyLocation
          },
          success: function(result) {
            // 성공한 경우 처리
            console.log('성공!');
            window.location.href = '/list';
          },
          error: function(xhr, textStatus, errorThrown) {
            console.log('실패');
            alert('사용할 수 없는 ID 입니다!!!');
            window.location.reload(true);
          }
        });
      });
  </script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
</body>
</html>
